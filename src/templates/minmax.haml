.minmax
  .boardMaster(style="width: {{boardMaster.const.rank() * 40}}px;")
    .score
      .score-item.black.col-xs-6(ng-class="{'current-turn':boardMaster.current.turn(boardMaster.const.TURN.H) && !boardMaster.isFinished()}")
        %i.fa.fa-star-o(ng-show="boardMaster.isFinished() && boardMaster.current.winner(boardMaster.const.TURN.H)")
        {{ boardMaster.current.score(boardMaster.const.TURN.H) }}
      .score-item.white.col-xs-6(ng-class="{'current-turn':boardMaster.current.turn(boardMaster.const.TURN.V) && !boardMaster.isFinished()}")
        %i.fa.fa-star-o(ng-show="boardMaster.isFinished() && boardMaster.current.winner(boardMaster.const.TURN.V)")
        {{ boardMaster.current.score(boardMaster.const.TURN.V) }}
    .board
      .row(ng-repeat="i in rankList")
        .panel.col-md-1(ng-repeat="j in rankList" ng-class="{selectable: boardMaster.selectable([i, j]), used: boardMaster.used([i, j])}" ng-click="clickCell(i, j)" style="width: 40px; height: 40px;")
          {{ boardMaster.get([i, j]) }}
  .menu(ng-show="boardMaster.isFinished()")
    .commands
      .command
        %button.btn.btn-default(ng-click="play()") {{ "replay"|trans }}
    .configs
      .config.form-group
        %label.col-sm-1 {{ "level"|trans }}
        %select.form-control(ng-model="p2_level" ng-options="v for v in levels")
  
